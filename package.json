{
  "name": "pants",
  "type": "module",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "prettier": "prettier . -w",
    "prettier-check": "prettier . --check",
    "build": "web-ext build",
    "web-ext-check": "web-ext lint"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "web-ext": "^8.2.0"
  }
}
