{
  "name": "pants",
  "type": "module",
  "private": true,
  "version": "0.1.1",
  "scripts": {
    "prettier": "prettier . -w",
    "prettier-check": "prettier . --check",
    "build": "web-ext build",
    "web-ext-check": "web-ext lint",
    "publish": "web-ext sign --channel listed --amo-metadata amo.json"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "web-ext": "^8.2.0"
  }
}
